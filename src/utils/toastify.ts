import { ToastOptions } from "react-toastify";

// toast function (from react-toastiy) options
export const toastOptions: ToastOptions = {
  position: "top-right",
  autoClose: 2500, // 2 seconds and 500 miliseconds
  hideProgressBar: false,
  closeOnClick: true,
  draggable: true,
  pauseOnHover: false,
};
